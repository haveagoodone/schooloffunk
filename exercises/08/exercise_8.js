/**
 * TASK:
 * Retrieve id and title, and the name of the first attribute of every advice
 */
const getAdviceAttributes = (sections) => {
  // Your code goes here:
};

module.exports = getAdviceAttributes;
